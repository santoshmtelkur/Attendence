# Base URL of the Spring Boot backend.
# Local dev default (matches backend's default port 8080):
VITE_API_URL=http://localhost:8080

# When deployed, point this at your hosted backend, e.g.:
# VITE_API_URL=https://attendo-backend.onrender.com
